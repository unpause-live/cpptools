version: 2
jobs:
  build:
    docker:
      - image: frolvlad/alpine-gxx:latest
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: build tests
          command: |
            pushd test
            make
            popd
      - run:
          name: run tests
          command: |
            pushd test
            make test
            popd
